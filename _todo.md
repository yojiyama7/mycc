- `rm -f ~/.docker/config.json`
  - これあると邪魔らしい なにこれ

---

- [x] step1 1つの整数をコンパイルする
- [x] step2 加減算の実装
  - [x] トークナイザの導入
- [x] エラーメッセージのリッチ化
  - 結構シンプルに行えるもんやな
- [x] parseの基本構造
- [x] unary +, -
- [x] 比較演算
- [ ] step9 ローカル変数
  - [x] a~zまでをローカル変数として使えるようにする(in main)
  - [x] 複数文字をラベルとして使用できるようにする
- [x] returnの実装
- [x] 変数名に_を使えるようにする
- [x] 変数名の2文字目以降に数字を使えるようにする
- [x] if for while
- [x] 引数なし関数の呼び出しをできるようにする
  - ここでは定義はできなくてよい
- [x] 6個以内の引数を関数呼び出しで渡せるようにする
- [x] mainのローカル変数が26個固定になっているのを直す
- [x] for, while の実行部分のフィールドをbodyからthenへ移動する
- [x] ptr + int の int に倍率をかける
- [ ] ~~パースした型をcodegenに反映させる~~
  - codegenには反映しなかったがsolve_typeという形で必要なときに解析、取得するようにした
- [ ] ローカル変数の追加を関数で切り出す
- [ ] sizeofに対して型名を入力できるようにする
- [ ] 配列 = ptr をエラーにする